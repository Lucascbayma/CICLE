import React from 'react';

function Usuario() {
    return (
        <div className="login-background" style={{ height: '100vh', width: '100vw', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
            <h1 style={{ color: 'white' }}>Página do Usuário em Construção</h1>
        </div>
    );
}

export default Usuario;